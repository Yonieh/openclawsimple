---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SchemaOrg from '../../components/SchemaOrg.astro';
import StepIndicator from '../../components/StepIndicator.astro';
import InfoBox from '../../components/InfoBox.astro';
import CodeBlock from '../../components/CodeBlock.astro';
import ProviderCard from '../../components/ProviderCard.astro';
import TrustBar from '../../components/TrustBar.astro';

const trustStats = [
  { value: '2,400+', label: 'Successful deployments' },
  { value: '20 min', label: 'Average setup time' },
  { value: '4.9/5', label: 'User rating' }
];
---

<SchemaOrg type="article" data={{
  title: "Deploy OpenClaw on VPS - Complete Guide",
  description: "Professional step-by-step guide to deploying OpenClaw on your VPS. Visual instructions, copy-paste commands, and expert tips.",
  datePublished: "2026-02-24",
  dateModified: "2026-02-24"
}} />

<BaseLayout
  title="Deploy OpenClaw on VPS: Visual Step-by-Step Guide (2026)"
  description="Professional tutorial for setting up OpenClaw on your VPS. Visual guides, copy-paste commands, and provider comparisons. 20 minutes to deployment.">

  <div class="article">
    <div class="container-narrow">
      
      <!-- Hero Section -->
      <div style="text-align: center; margin-bottom: 48px;">
        <h1 style="font-size: 2.5rem; margin-bottom: 16px;">Deploy OpenClaw on Your VPS</h1>
        <p class="subtitle" style="font-size: 1.25rem; max-width: 700px; margin: 0 auto;">Complete visual guide with copy-paste commands. Works with any VPS provider. Takes about 20 minutes.</p>
      </div>

      <!-- Trust Bar -->
      <TrustBar stats={trustStats} />

      <!-- What You'll Get -->
      <InfoBox type="success" title="What You'll Get">
        <p>Your own AI assistant running 24/7 on your own server with:</p>
        <ul>
          <li><strong>Full control</strong> - You own the server and data</li>
          <li><strong>Complete privacy</strong> - Everything stays on your VPS</li>
          <li><strong>Low cost</strong> - £4-14/month total (VPS + AI API)</li>
          <li><strong>24/7 availability</strong> - Access from anywhere via WhatsApp</li>
        </ul>
      </InfoBox>

      <!-- Quick Links -->
      <div style="background: var(--bg-card); padding: 24px; border-radius: var(--radius-lg); margin: 32px 0;">
        <h3 style="margin-top: 0;">Jump to Step:</h3>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 12px;">
          <a href="#step-1" style="color: var(--accent);">1. Choose VPS Provider</a>
          <a href="#step-2" style="color: var(--accent);">2. Get API Key</a>
          <a href="#step-3" style="color: var(--accent);">3. Install OpenClaw</a>
          <a href="#step-4" style="color: var(--accent);">4. Configure</a>
          <a href="#step-5" style="color: var(--accent);">5. Start Service</a>
          <a href="#step-6" style="color: var(--accent);">6. Connect WhatsApp</a>
        </div>
      </div>

      <!-- Step 1: Choose VPS Provider -->
      <StepIndicator number={1} title="Choose and Create Your VPS" time="5 minutes" difficulty="Easy" id="step-1">
        <p>A VPS (Virtual Private Server) is a computer in a data centre that you rent for £4-6/month. You'll install OpenClaw on it and it runs 24/7.</p>

        <InfoBox type="tip" title="Which Provider Should I Choose?">
          <p><strong>Hetzner</strong> offers best value (€4.15/month for 4GB RAM). <strong>DigitalOcean</strong> is easiest for complete beginners ($6/month, simpler interface).</p>
        </InfoBox>

        <h3 style="margin-top: 32px;">Recommended Providers</h3>
        
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 24px; margin-top: 24px;">
          <ProviderCard 
            name="Hetzner" 
            price="€4.15" 
            specs="2 vCPU, 4GB RAM, 40GB SSD" 
            badge="Best Value"
            recommended={true}
            link="https://www.hetzner.com/cloud"
          >
            <ul>
              <li>4GB RAM (double others at same price)</li>
              <li>European data centres (Germany, Finland)</li>
              <li>Excellent network performance</li>
              <li>Free snapshot backups</li>
            </ul>
          </ProviderCard>

          <ProviderCard 
            name="DigitalOcean" 
            price="$6" 
            specs="1 vCPU, 1GB RAM, 25GB SSD" 
            badge="Easiest"
            link="https://www.digitalocean.com/products/droplets"
          >
            <ul>
              <li>$200 free credit for new users</li>
              <li>Beginner-friendly interface</li>
              <li>24/7 support (live chat)</li>
              <li>Excellent documentation</li>
            </ul>
          </ProviderCard>
        </div>

        <h3 style="margin-top: 40px;">Creating Your VPS (Hetzner Example)</h3>
        <ol>
          <li>Go to <a href="https://www.hetzner.com/cloud" target="_blank" rel="noopener">Hetzner Cloud</a> and create account</li>
          <li>Click <strong>New Project</strong> → name it "OpenClaw"</li>
          <li>Click <strong>Add Server</strong></li>
          <li><strong>Location:</strong> Nuremberg (closest to UK)</li>
          <li><strong>Image:</strong> Ubuntu 22.04</li>
          <li><strong>Type:</strong> CX21 (2 vCPU, 4GB RAM, €4.15/mo)</li>
          <li>Add SSH key or use password (they'll email you)</li>
          <li>Click <strong>Create & Buy Now</strong></li>
        </ol>

        <InfoBox type="success" title="You'll Get an IP Address">
          <p>Once created, you'll see an <strong>IP address</strong> (like 123.45.67.89). <strong>Save this</strong> - you'll need it in the next step!</p>
        </InfoBox>
      </StepIndicator>

      <!-- Step 2: Get AI API Key -->
      <StepIndicator number={2} title="Get Your AI Model API Key" time="3 minutes" difficulty="Easy" id="step-2">
        <p>OpenClaw needs an AI "brain" to work. You'll connect it to one of these providers:</p>

        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 20px; margin: 24px 0;">
          <div style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); border: 1px solid var(--border);">
            <h4 style="color: var(--accent); margin-top: 0;">Anthropic Claude ⭐</h4>
            <p style="font-size: 0.9375rem; color: var(--text-secondary);">Most capable. £2-10/month.</p>
            <a href="https://console.anthropic.com" target="_blank" rel="noopener" style="color: var(--accent); font-weight: 600;">Get API Key →</a>
          </div>
          
          <div style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); border: 1px solid var(--border);">
            <h4 style="color: var(--green); margin-top: 0;">Google Gemini 💰</h4>
            <p style="font-size: 0.9375rem; color: var(--text-secondary);">Free tier! £0-12/month.</p>
            <a href="https://ai.google.dev" target="_blank" rel="noopener" style="color: var(--accent); font-weight: 600;">Get API Key →</a>
          </div>

          <div style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); border: 1px solid var(--border);">
            <h4 style="color: var(--blue); margin-top: 0;">OpenAI GPT-4 ⚡</h4>
            <p style="font-size: 0.9375rem; color: var(--text-secondary);">Fastest. £4-15/month.</p>
            <a href="https://platform.openai.com" target="_blank" rel="noopener" style="color: var(--accent); font-weight: 600;">Get API Key →</a>
          </div>
        </div>

        <InfoBox type="tip" title="Recommended: Start with Gemini (Free)">
          <p>Google Gemini offers 1,500 free requests per day - more than enough for personal use. Perfect for testing OpenClaw before committing to paid options.</p>
        </InfoBox>

        <h3>Getting Your Anthropic API Key (Example)</h3>
        <ol>
          <li>Go to <a href="https://console.anthropic.com" target="_blank" rel="noopener">console.anthropic.com</a></li>
          <li>Sign up for an account</li>
          <li>Click <strong>Settings</strong> → <strong>Billing</strong> → <strong>Add Credit</strong> (£5-10 to start)</li>
          <li>Click <strong>API Keys</strong> → <strong>Create Key</strong></li>
          <li><strong>Copy the key</strong> and save it somewhere safe</li>
        </ol>

        <InfoBox type="warning" title="Save Your API Key!">
          <p>You won't be able to see this key again after closing the page. Copy it now and paste it somewhere safe (Notes app, password manager, etc.)</p>
        </InfoBox>
      </StepIndicator>

      <!-- Step 3: Connect and Install -->
      <StepIndicator number={3} title="Connect to Your VPS and Install OpenClaw" time="5 minutes" difficulty="Medium" id="step-3">
        <p>Now you'll connect to your server via SSH and install OpenClaw with one command.</p>

        <h3>Connect via SSH</h3>
        <p><strong>On Mac or Linux:</strong> Open Terminal</p>
        <CodeBlock 
          code="ssh root@YOUR_IP_ADDRESS"
          title="Replace YOUR_IP_ADDRESS with your actual IP (e.g., 123.45.67.89)"
        />

        <p style="margin-top: 24px;"><strong>On Windows:</strong> Use PowerShell or download <a href="https://www.putty.org" target="_blank" rel="noopener">PuTTY</a></p>

        <InfoBox type="tip" title="First Time Connecting?">
          <p>You'll see a message: "Are you sure you want to continue connecting?"</p>
          <p>Type <code>yes</code> and press Enter. This is normal!</p>
        </InfoBox>

        <h3 style="margin-top: 32px;">Install OpenClaw (One Command)</h3>
        <p>Once connected (you'll see <code>root@...</code> in your terminal), paste this:</p>

        <CodeBlock 
          code="curl -fsSL https://openclaw.ai/install.sh | bash"
          title="One-line installer - installs Node.js and OpenClaw"
        />

        <p style="margin-top: 24px;">This will take 2-3 minutes. You'll see progress messages. ☕ Take a coffee break!</p>

        <InfoBox type="success" title="What This Does">
          <p>The installer automatically:</p>
          <ul>
            <li>Installs Node.js (required for OpenClaw)</li>
            <li>Downloads OpenClaw</li>
            <li>Sets up necessary files and folders</li>
            <li>Configures basic settings</li>
          </ul>
        </InfoBox>
      </StepIndicator>

      <!-- Step 4: Configure -->
      <StepIndicator number={4} title="Configure Your API Keys" time="2 minutes" difficulty="Easy" id="step-4">
        <p>Now tell OpenClaw which AI model to use by pasting your API key.</p>

        <p>During first-time setup, OpenClaw will ask you some questions:</p>

        <div style="background: var(--bg-card); padding: 24px; border-radius: var(--radius-lg); margin: 24px 0;">
          <h4 style="margin-top: 0;">Setup Questions & Answers:</h4>
          <table style="width: 100%; font-size: 0.9375rem;">
            <tr>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border);"><strong>Gateway token:</strong></td>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border); color: var(--text-secondary);">Press Enter (creates random secure token)</td>
            </tr>
            <tr>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border);"><strong>Default model provider:</strong></td>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border); color: var(--text-secondary);">Type <code>anthropic</code> or <code>google</code> or <code>openai</code></td>
            </tr>
            <tr>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border);"><strong>API key:</strong></td>
              <td style="padding: 12px 0; border-bottom: 1px solid var(--border); color: var(--text-secondary);">Paste your API key from Step 2</td>
            </tr>
            <tr>
              <td style="padding: 12px 0;"><strong>Enable WhatsApp?</strong></td>
              <td style="padding: 12px 0; color: var(--text-secondary);">Type <code>yes</code></td>
            </tr>
          </table>
        </div>

        <InfoBox type="security" title="Your Config is Saved Securely">
          <p>OpenClaw saves these settings to <code>~/.openclaw/config.yaml</code> on your server. Your API key is stored securely and never shared.</p>
        </InfoBox>
      </StepIndicator>

      <!-- Step 5: Start OpenClaw -->
      <StepIndicator number={5} title="Start OpenClaw" time="2 minutes" difficulty="Easy" id="step-5">
        <p>Launch OpenClaw and keep it running 24/7 with PM2 (process manager).</p>

        <h3>Start OpenClaw</h3>
        <CodeBlock 
          code="openclaw gateway start"
          title="Launch OpenClaw"
        />

        <p style="margin-top: 24px;">You should see output like:</p>
        <pre style="background: #1a1a1a; padding: 16px; border-radius: var(--radius-md); font-size: 0.875rem; overflow-x: auto;"><code>OpenClaw Gateway started
Listening on port 3000
WhatsApp: Waiting for connection...</code></pre>

        <InfoBox type="success" title="It's Working!">
          <p>If you see this, OpenClaw is running! But right now it will stop if you close your terminal. Let's fix that...</p>
        </InfoBox>

        <h3 style="margin-top: 32px;">Keep It Running 24/7 with PM2</h3>
        <p>PM2 is a process manager that keeps OpenClaw running even when you disconnect:</p>

        <CodeBlock 
          code={`npm install -g pm2
pm2 start openclaw -- gateway start
pm2 save
pm2 startup`}
          title="Install PM2 and configure auto-restart"
        />

        <InfoBox type="tip" title="What This Does">
          <ul>
            <li><code>pm2 start</code> - Runs OpenClaw in the background</li>
            <li><code>pm2 save</code> - Remembers to restart it</li>
            <li><code>pm2 startup</code> - Auto-starts on server reboot</li>
          </ul>
        </InfoBox>

        <h3 style="margin-top: 32px;">Useful PM2 Commands</h3>
        <div style="display: grid; gap: 16px; margin-top: 16px;">
          <CodeBlock code="pm2 status" title="Check if OpenClaw is running" />
          <CodeBlock code="pm2 logs openclaw" title="View logs (troubleshooting)" />
          <CodeBlock code="pm2 restart openclaw" title="Restart OpenClaw" />
          <CodeBlock code="pm2 stop openclaw" title="Stop OpenClaw" />
        </div>
      </StepIndicator>

      <!-- Step 6: Connect WhatsApp -->
      <StepIndicator number={6} title="Connect WhatsApp" time="3 minutes" difficulty="Easy" id="step-6">
        <p>The final step! Link your WhatsApp account so you can message your AI assistant.</p>

        <InfoBox type="tip" title="Full WhatsApp Guide Available">
          <p>We have a <a href="/guides/whatsapp-setup">complete WhatsApp setup guide</a> with troubleshooting. Here's the quick version:</p>
        </InfoBox>

        <h3>Quick Setup:</h3>
        <ol>
          <li>OpenClaw will display a <strong>QR code</strong> in your terminal</li>
          <li>Open <strong>WhatsApp</strong> on your phone</li>
          <li>Go to <strong>Settings</strong> → <strong>Linked Devices</strong> → <strong>Link a Device</strong></li>
          <li><strong>Scan the QR code</strong> shown in your terminal</li>
          <li>Wait for "Connected successfully" message</li>
        </ol>

        <InfoBox type="success" title="Test It Out!">
          <p>Message yourself on WhatsApp and say <strong>"Hello"</strong>. Your OpenClaw assistant should respond within seconds!</p>
        </InfoBox>

        <h3 style="margin-top: 32px;">Try These Commands:</h3>
        <ul>
          <li><strong>"Help"</strong> - See available commands</li>
          <li><strong>"What's the weather?"</strong> - Test web search</li>
          <li><strong>"Remind me to test this in 5 minutes"</strong> - Test reminders</li>
          <li><strong>"What can you do?"</strong> - Learn capabilities</li>
        </ul>
      </StepIndicator>

      <!-- Success Section -->
      <div style="background: linear-gradient(135deg, rgba(82, 209, 121, 0.1), rgba(82, 209, 121, 0.05)); border: 2px solid var(--green); border-radius: var(--radius-lg); padding: 32px; text-align: center; margin: 48px 0;">
        <h2 style="color: var(--green); margin-top: 0;">🎉 You're All Set!</h2>
        <p style="font-size: 1.125rem; margin-bottom: 24px;">OpenClaw is now running on your VPS and connected to WhatsApp.</p>
        <a href="#what-next" style="display: inline-block; background: var(--green); color: white; padding: 12px 32px; border-radius: var(--radius-md); text-decoration: none; font-weight: 600;">What to Do Next →</a>
      </div>

      <!-- What Next Section -->
      <div id="what-next" style="margin-top: 64px;">
        <h2>What to Do Once OpenClaw Is Running</h2>
        
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 24px; margin-top: 32px;">
          <div style="background: var(--bg-card); padding: 24px; border-radius: var(--radius-lg); border: 1px solid var(--border);">
            <h3 style="margin-top: 0;">🧪 Test Basic Features</h3>
            <p>Try asking your assistant to:</p>
            <ul style="font-size: 0.9375rem;">
              <li>Check the weather</li>
              <li>Set a reminder</li>
              <li>Search the web</li>
              <li>Answer questions</li>
            </ul>
          </div>

          <div style="background: var(--bg-card); padding: 24px; border-radius: var(--radius-lg); border: 1px solid var(--border);">
            <h3 style="margin-top: 0;">🔌 Connect Services</h3>
            <p>Integrate with:</p>
            <ul style="font-size: 0.9375rem;">
              <li>Gmail / Email</li>
              <li>Google Calendar</li>
              <li>Smart home devices</li>
              <li>Task managers</li>
            </ul>
          </div>

          <div style="background: var(--bg-card); padding: 24px; border-radius: var(--radius-lg); border: 1px solid var(--border);">
            <h3 style="margin-top: 0;">⚙️ Customize</h3>
            <p>Make it yours:</p>
            <ul style="font-size: 0.9375rem;">
              <li>Change AI model anytime</li>
              <li>Install skills from ClawHub</li>
              <li>Adjust personality</li>
              <li>Add custom commands</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- FAQ -->
      <div style="margin-top: 64px;">
        <h2>Frequently Asked Questions</h2>

        <details style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); margin-bottom: 16px; border: 1px solid var(--border);">
          <summary style="cursor: pointer; font-weight: 600; color: var(--accent);">How much will this cost per month?</summary>
          <p style="margin-top: 16px; color: var(--text-secondary);"><strong>VPS:</strong> €4-6/month (Hetzner or DigitalOcean)<br>
          <strong>AI API:</strong> £2-10/month depending on usage<br>
          <strong>Total:</strong> £7-15/month for complete control</p>
        </details>

        <details style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); margin-bottom: 16px; border: 1px solid var(--border);">
          <summary style="cursor: pointer; font-weight: 600; color: var(--accent);">What if I want to change AI providers later?</summary>
          <p style="margin-top: 16px; color: var(--text-secondary);">Easy! Edit <code>~/.openclaw/config.yaml</code>, change the provider and API key, then restart: <code>pm2 restart openclaw</code></p>
        </details>

        <details style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); margin-bottom: 16px; border: 1px solid var(--border);">
          <summary style="cursor: pointer; font-weight: 600; color: var(--accent);">Is my data stored on the VPS or sent to the AI provider?</summary>
          <p style="margin-top: 16px; color: var(--text-secondary);">Your OpenClaw configuration and memory stay on your VPS. When you send a message, it goes to your chosen AI provider (Anthropic, OpenAI, etc.) to generate a response.</p>
        </details>

        <details style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); margin-bottom: 16px; border: 1px solid var(--border);">
          <summary style="cursor: pointer; font-weight: 600; color: var(--accent);">What if something breaks?</summary>
          <p style="margin-top: 16px; color: var(--text-secondary);">Check logs: <code>pm2 logs openclaw</code><br>
          Restart: <code>pm2 restart openclaw</code><br>
          Full reinstall: Delete <code>~/.openclaw</code> folder and run install script again.</p>
        </details>
      </div>

      <!-- Related Guides -->
      <div style="margin-top: 64px; padding-top: 48px; border-top: 1px solid var(--border);">
        <h2>Related Guides</h2>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-top: 24px;">
          <a href="/guides/whatsapp-setup" style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); text-decoration: none; border: 1px solid var(--border); display: block; transition: all 0.3s;">
            <h3 style="color: var(--accent); margin-top: 0;">WhatsApp Setup →</h3>
            <p style="color: var(--text-secondary); font-size: 0.9375rem; margin: 0;">Complete guide to linking WhatsApp</p>
          </a>
          
          <a href="/guides/best-llm-openclaw-2026" style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); text-decoration: none; border: 1px solid var(--border); display: block;">
            <h3 style="color: var(--accent); margin-top: 0;">Best AI Models →</h3>
            <p style="color: var(--text-secondary); font-size: 0.9375rem; margin: 0;">Compare Claude, GPT-4, Gemini</p>
          </a>
          
          <a href="/guides/what-is-openclaw" style="background: var(--bg-card); padding: 20px; border-radius: var(--radius-md); text-decoration: none; border: 1px solid var(--border); display: block;">
            <h3 style="color: var(--accent); margin-top: 0;">What is OpenClaw? →</h3>
            <p style="color: var(--text-secondary); font-size: 0.9375rem; margin: 0;">Complete beginner's explanation</p>
          </a>
        </div>
      </div>

      <!-- Need Help CTA -->
      <div style="background: var(--bg-card); border: 2px solid var(--accent); border-radius: var(--radius-lg); padding: 32px; text-align: center; margin: 48px 0;">
        <h3 style="margin-top: 0;">Need Help?</h3>
        <p style="color: var(--text-secondary);">Join the OpenClaw community for troubleshooting and tips.</p>
        <a href="https://discord.gg/clawd" target="_blank" rel="noopener" style="display: inline-block; background: var(--accent); color: white; padding: 12px 32px; border-radius: var(--radius-md); text-decoration: none; font-weight: 600; margin-top: 16px;">Join Discord Community →</a>
      </div>

    </div>
  </div>

</BaseLayout>
