---
// OpenClaw Simple - Accessible, Stylish Homepage
import SchemaOrg from '../components/SchemaOrg.astro';
---
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>OpenClaw Setup Guide: Install in 5 Minutes | OpenClaw Simple</title>
<meta name="description" content="Complete beginner's guide to setting up OpenClaw - your AI assistant that checks emails, books restaurants, and manages your calendar from WhatsApp. Free and paid options.">
<link rel="canonical" href="https://openclawsimple.com" />
<meta property="og:title" content="Set Up OpenClaw in 5 Minutes">
<meta property="og:description" content="Your AI assistant that checks emails, books restaurants, and manages your calendar from WhatsApp.">
<meta property="og:url" content="https://openclawsimple.com">
<meta property="og:type" content="website">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="OpenClaw Setup Guide: Install in 5 Minutes">
<meta name="twitter:description" content="Complete beginner's guide. Free and paid setup options. Your AI assistant that actually does things.">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<SchemaOrg type="website" />
<style>
  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  :root {
    --bg: #0d0d12;
    --bg-elevated: #16161d;
    --bg-card: #1c1c26;
    --bg-card-hover: #24242f;
    --border: #2d2d3d;
    --border-subtle: #222230;
    --text: #f5f5f7;
    --text-secondary: #a8a8b8;
    --text-tertiary: #75758a;
    --accent: #ff6b57;
    --accent-hover: #ff8574;
    --accent-glow: rgba(255, 107, 87, 0.25);
    --green: #52d179;
    --blue: #5b9eff;
    --font-body: 'Inter', -apple-system, sans-serif;
    --radius-md: 12px;
    --radius-lg: 16px;
    --radius-xl: 24px;
    --space-md: 1rem;
    --space-lg: 1.5rem;
    --space-xl: 2rem;
    --space-2xl: 3rem;
    --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
    --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
    --shadow-lg: 0 12px 32px rgba(0, 0, 0, 0.5);
    --shadow-glow: 0 0 32px var(--accent-glow);
  }

  html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; }
  body {
    font-family: var(--font-body);
    background: var(--bg);
    color: var(--text);
    line-height: 1.6;
    overflow-x: hidden;
  }

  *:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* Screen reader only text */
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }

  /* SKIP TO CONTENT */
  .skip-to-main {
    position: absolute;
    left: -9999px;
    top: 0;
    z-index: 999;
    padding: var(--space-md);
    background: var(--accent);
    color: white;
    text-decoration: none;
    font-weight: 600;
    border-radius: var(--radius-md);
  }
  .skip-to-main:focus {
    left: var(--space-md);
    top: var(--space-md);
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: var(--space-md) var(--space-lg);
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(13, 13, 18, 0.85);
    backdrop-filter: blur(24px);
    border-bottom: 1px solid var(--border-subtle);
    box-shadow: var(--shadow-sm);
  }

  .nav-logo {
    display: flex;
    align-items: center;
    gap: 12px;
    font-weight: 700;
    font-size: 1.125rem;
    color: var(--text);
    text-decoration: none;
    transition: opacity 0.2s;
  }

  .nav-logo:hover { opacity: 0.8; }
  .nav-logo .lobster { font-size: 1.5rem; filter: drop-shadow(0 0 12px rgba(255, 107, 87, 0.5)); }

  .nav-cta {
    padding: 12px 24px;
    background: var(--accent);
    color: white;
    font-weight: 600;
    font-size: 0.875rem;
    border: none;
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all 0.2s;
    box-shadow: var(--shadow-sm);
  }

  .nav-cta:hover {
    background: var(--accent-hover);
    transform: translateY(-1px);
    box-shadow: var(--shadow-md), var(--shadow-glow);
  }

  .container { max-width: 1120px; margin: 0 auto; padding: 0 var(--space-lg); }

  /* HERO */
  .hero {
    padding: 140px 0 80px;
    text-align: center;
    position: relative;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -100px;
    left: 50%;
    transform: translateX(-50%);
    width: 700px;
    height: 700px;
    background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 8px 20px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 100px;
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin-bottom: 32px;
    font-weight: 500;
  }

  .hero-badge .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 8px var(--green);
    animation: pulse 2s ease infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 24px;
  }

  h1 .highlight {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-sub {
    font-size: 1.25rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 48px;
    line-height: 1.6;
  }

  .hero-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 56px;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 16px 32px;
    background: var(--accent);
    color: white;
    font-weight: 600;
    font-size: 1.125rem;
    border: none;
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all 0.2s ease;
    box-shadow: var(--shadow-md);
  }

  .btn-primary:hover {
    background: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg), var(--shadow-glow);
  }

  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 16px 32px;
    background: var(--bg-card);
    color: var(--text);
    font-weight: 600;
    font-size: 1.125rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-secondary:hover {
    background: var(--bg-card-hover);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  /* TRUST BAR - now a proper list */
  .trust-bar {
    display: flex;
    justify-content: center;
    gap: 40px;
    flex-wrap: wrap;
    margin-bottom: 72px;
    list-style: none;
  }

  .trust-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.9375rem;
    color: var(--text-tertiary);
    font-weight: 500;
  }

  /* CHAT DEMO */
  .chat-demo {
    max-width: 480px;
    margin: 0 auto 100px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
  }

  .chat-header {
    padding: 16px 24px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 0.9375rem;
    font-weight: 600;
    background: var(--bg-elevated);
  }

  .chat-header .status {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 8px var(--green);
  }

  .chat-messages {
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .msg {
    max-width: 85%;
    padding: 12px 16px;
    border-radius: 16px;
    font-size: 0.9375rem;
    line-height: 1.5;
    animation: fadeUp 0.5s ease both;
  }

  .msg:nth-child(1) { animation-delay: 0.2s; }
  .msg:nth-child(2) { animation-delay: 0.6s; }
  .msg:nth-child(3) { animation-delay: 1.0s; }
  .msg:nth-child(4) { animation-delay: 1.4s; }
  .msg:nth-child(5) { animation-delay: 1.8s; }
  .msg:nth-child(6) { animation-delay: 2.2s; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .msg-user {
    align-self: flex-end;
    background: var(--accent);
    color: white;
    border-bottom-right-radius: 4px;
  }

  .msg-bot {
    align-self: flex-start;
    background: var(--bg-elevated);
    color: var(--text);
    border-bottom-left-radius: 4px;
    border: 1px solid var(--border);
  }

  .msg-bot .check {
    color: var(--green);
    font-weight: 600;
  }

  /* SECTIONS */
  .section-title {
    text-align: center;
    margin-bottom: 16px;
    font-size: clamp(1.875rem, 4vw, 2.5rem);
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .section-sub {
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.125rem;
    margin-bottom: 56px;
  }

  #start { scroll-margin-top: 100px; padding: 80px 0; }
  #what-is { scroll-margin-top: 100px; padding: 80px 0; background: var(--bg-elevated); }

  /* PATHS */
  .paths {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px;
    margin-bottom: var(--space-2xl);
  }

  .path-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    padding: 32px;
    position: relative;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .path-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--border);
  }

  .path-card.recommended {
    border-color: var(--accent);
    box-shadow: 0 0 0 1px var(--accent), var(--shadow-md), var(--shadow-glow);
  }

  .path-badge {
    position: absolute;
    top: -14px;
    left: 32px;
    padding: 6px 16px;
    background: var(--accent);
    color: white;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-md);
  }

  .path-icon { font-size: 2.5rem; margin-bottom: 20px; }
  .path-card h3 { font-size: 1.5rem; font-weight: 700; margin-bottom: 12px; }
  .path-card .desc {
    font-size: 1rem;
    color: var(--text-secondary);
    margin-bottom: 28px;
    line-height: 1.6;
  }

  /* PATH STATS - now a definition list */
  .path-stats {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 28px;
    flex: 1;
  }

  .path-stat {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.9375rem;
  }

  .path-stat dt { color: var(--text-secondary); }
  .path-stat dd { font-weight: 600; color: var(--text); margin: 0; }

  .path-link {
    display: block;
    text-align: center;
    padding: 14px;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    color: var(--text);
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.2s;
  }

  .path-link:hover {
    background: var(--bg-card-hover);
    transform: translateY(-1px);
  }

  .path-card.recommended .path-link {
    background: var(--accent);
    border-color: var(--accent);
    color: white;
  }

  .path-card.recommended .path-link:hover {
    background: var(--accent-hover);
  }

  .path-link.disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* FEATURES */
  .what-is-content {
    max-width: 720px;
    margin: 0 auto 64px;
    text-align: center;
  }

  .what-is-content p {
    font-size: 1.125rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 20px;
  }

  .what-is-content strong { color: var(--text); }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 20px;
  }

  .feature-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 28px;
    transition: all 0.3s;
  }

  .feature-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .feature-icon { font-size: 2rem; margin-bottom: 16px; }
  .feature-card h4 { font-size: 1.125rem; font-weight: 600; margin-bottom: 8px; }
  .feature-card p { font-size: 0.9375rem; color: var(--text-secondary); line-height: 1.6; margin: 0; }

  /* FINAL CTA */
  .final-cta {
    text-align: center;
    padding: 100px 0;
  }

  .final-cta h2 {
    font-size: clamp(1.875rem, 4vw, 2.5rem);
    font-weight: 800;
    margin-bottom: 16px;
  }

  .final-cta p {
    color: var(--text-secondary);
    font-size: 1.125rem;
    margin-bottom: 40px;
  }

  /* FOOTER */
  footer {
    border-top: 1px solid var(--border-subtle);
    padding: var(--space-2xl) 0;
    background: var(--bg-elevated);
  }

  .footer-logo {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    font-weight: 700;
    font-size: 1.125rem;
    margin-bottom: 12px;
  }

  .footer-tagline {
    font-size: 0.875rem;
    color: var(--text-tertiary);
    margin-bottom: 20px;
    text-align: center;
  }

  .footer-links {
    display: flex;
    justify-content: center;
    gap: 32px;
    flex-wrap: wrap;
    margin-bottom: 24px;
  }

  .footer-links a {
    font-size: 0.875rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s;
  }

  .footer-links a:hover { color: var(--text); }

  .footer-disclaimer {
    font-size: 0.75rem;
    color: var(--text-tertiary);
    text-align: center;
  }

  @media (max-width: 768px) {
    nav { padding: var(--space-md); }
    .hero { padding: 120px 0 60px; }
    .trust-bar { gap: 24px; }
    .paths { grid-template-columns: 1fr; }
    .hero-buttons { flex-direction: column; align-items: center; }
    .btn-primary, .btn-secondary { width: 100%; max-width: 320px; justify-content: center; }
  }

  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>
</head>
<body>

<!-- Skip to main content for keyboard/screen reader users -->
<a href="#main-content" class="skip-to-main">Skip to main content</a>

<nav role="navigation" aria-label="Main navigation">
  <a href="/" class="nav-logo" aria-label="OpenClaw Simple — home">
    <span class="lobster" aria-hidden="true">🦞</span>
    <span>OpenClaw Simple</span>
  </a>
  <a href="#start" class="nav-cta">Get Started</a>
</nav>

<main id="main-content">

<!-- HERO -->
<section class="hero" aria-labelledby="hero-heading">
  <div class="container">
    <div class="hero-badge">
      <span class="dot" aria-hidden="true"></span>
      Complete beginner's guide
    </div>

    <h1 id="hero-heading">Set Up OpenClaw in <span class="highlight">5 Minutes</span></h1>

    <p class="hero-sub">
      Your own AI assistant that actually does things. Checks emails, books restaurants, manages your calendar — all from WhatsApp. We'll show you the easiest way.
    </p>

    <div class="hero-buttons">
      <a href="/guides/railway-quick-start" class="btn-primary">Get Started →</a>
      <a href="#what-is" class="btn-secondary">See How It Works</a>
    </div>

    <ul class="trust-bar" aria-label="Key benefits">
      <li class="trust-item"><span aria-hidden="true">⚡</span> Easy to follow</li>
      <li class="trust-item"><span aria-hidden="true">💰</span> Free option available</li>
      <li class="trust-item"><span aria-hidden="true">🔒</span> Your data stays yours</li>
    </ul>

    <div class="chat-demo" role="figure" aria-label="Example conversation with OpenClaw on WhatsApp">
      <div class="chat-header">
        <span class="status" aria-hidden="true"></span>
        <span aria-hidden="true">🦞</span> OpenClaw — Online
      </div>
      <div class="chat-messages" aria-hidden="true">
        <div class="msg msg-user">Check my emails and flag anything urgent</div>
        <div class="msg msg-bot"><span class="check">✓</span> Checked — 3 urgent emails found</div>
        <div class="msg msg-user">Book me a table for 2 on Friday at 7pm</div>
        <div class="msg msg-bot"><span class="check">✓</span> Booking request sent</div>
        <div class="msg msg-user">Remind me to call Dave at 9am tomorrow</div>
        <div class="msg msg-bot"><span class="check">✓</span> Reminder set for 9:00 AM</div>
      </div>
    </div>
  </div>
</section>

<!-- SETUP PATHS -->
<section id="start" aria-labelledby="start-heading">
  <div class="container">
    <h2 class="section-title" id="start-heading">Choose Your Setup Method</h2>
    <p class="section-sub">Pick the approach that works best for you</p>

    <div class="paths">
      <article class="path-card recommended">
        <span class="path-badge">⚡ Easiest</span>
        <div class="path-icon" aria-hidden="true">✨</div>
        <h3>One-Click Setup</h3>
        <p class="desc">Just click a button and paste one thing. No terminal, no code.</p>
        <dl class="path-stats">
          <div class="path-stat">
            <dt>Time:</dt>
            <dd>5 minutes</dd>
          </div>
          <div class="path-stat">
            <dt>Cost:</dt>
            <dd>£5/month hosting</dd>
          </div>
          <div class="path-stat">
            <dt>Always on:</dt>
            <dd>24/7</dd>
          </div>
        </dl>
        <a href="/guides/railway-quick-start" class="path-link">Start Here →</a>
      </article>

      <article class="path-card">
        <div class="path-icon" aria-hidden="true">🛠️</div>
        <h3>Free Setup</h3>
        <p class="desc">Run it on your Mac or Windows computer. No monthly cost.</p>
        <dl class="path-stats">
          <div class="path-stat">
            <dt>Time:</dt>
            <dd>20 minutes</dd>
          </div>
          <div class="path-stat">
            <dt>Cost:</dt>
            <dd>Free</dd>
          </div>
          <div class="path-stat">
            <dt>Note:</dt>
            <dd>Computer stays on</dd>
          </div>
        </dl>
        <a href="/guides/local-install" class="path-link">Free Guide →</a>
      </article>

      <article class="path-card">
        <div class="path-icon" aria-hidden="true">🚀</div>
        <h3>Done For You</h3>
        <p class="desc">We handle everything. You just start using it.</p>
        <dl class="path-stats">
          <div class="path-stat">
            <dt>Time:</dt>
            <dd>2 minutes</dd>
          </div>
          <div class="path-stat">
            <dt>Cost:</dt>
            <dd>£20/month</dd>
          </div>
          <div class="path-stat">
            <dt>Work:</dt>
            <dd>None</dd>
          </div>
        </dl>
        <span class="path-link disabled" role="link" aria-disabled="true">Coming Soon</span>
      </article>
    </div>
  </div>
</section>

<!-- WHAT IS OPENCLAW -->
<section id="what-is" aria-labelledby="what-is-heading">
  <div class="container">
    <h2 class="section-title" id="what-is-heading">What is OpenClaw?</h2>

    <div class="what-is-content">
      <p>
        OpenClaw is an open-source AI assistant that <strong>actually does things</strong> for you. Unlike ChatGPT which just answers questions, OpenClaw connects to your email, calendar, and apps to handle boring admin work.
      </p>
      <p>
        You message it from WhatsApp like you'd message a person. It runs on your own server (or one you rent), so your data stays private. This guide shows you the easiest ways to set it up.
      </p>
    </div>

    <div class="features" role="list">
      <div class="feature-card" role="listitem">
        <div class="feature-icon" aria-hidden="true">📧</div>
        <h4>Email Management</h4>
        <p>Checks inbox, flags urgent messages, drafts replies</p>
      </div>
      <div class="feature-card" role="listitem">
        <div class="feature-icon" aria-hidden="true">📅</div>
        <h4>Calendar Control</h4>
        <p>Schedules meetings, sets reminders, checks availability</p>
      </div>
      <div class="feature-card" role="listitem">
        <div class="feature-icon" aria-hidden="true">🍽️</div>
        <h4>Bookings &amp; Reservations</h4>
        <p>Books restaurants, orders taxis, makes appointments</p>
      </div>
      <div class="feature-card" role="listitem">
        <div class="feature-icon" aria-hidden="true">🔒</div>
        <h4>Your Data, Your Control</h4>
        <p>Self-hosted, open source, no vendor lock-in</p>
      </div>
    </div>
  </div>
</section>

<!-- FINAL CTA -->
<section class="final-cta" aria-labelledby="cta-heading">
  <div class="container">
    <h2 id="cta-heading">Ready to Get Started?</h2>
    <p>Follow one of our guides and have OpenClaw running in minutes.</p>
    <a href="/guides/railway-quick-start" class="btn-primary">Start With the Easy Guide →</a>
  </div>
</section>

</main>

<!-- FOOTER -->
<footer role="contentinfo">
  <div class="container">
    <div class="footer-logo">
      <span aria-hidden="true">🦞</span>
      <span>OpenClaw Simple</span>
    </div>
    <p class="footer-tagline">Setup guides for normal humans</p>
    <nav class="footer-links" aria-label="Footer navigation">
      <a href="/guides/railway-quick-start">Quick Start</a>
      <a href="/guides/what-is-openclaw">What is OpenClaw</a>
      <a href="/guides/local-install">Free Setup</a>
      <a href="https://github.com/openclaw/openclaw" target="_blank" rel="noopener">GitHub <span class="sr-only">(opens in new tab)</span></a>
    </nav>
    <p class="footer-disclaimer">Independent resource · Not affiliated with OpenClaw project</p>
  </div>
</footer>

</body>
</html>
